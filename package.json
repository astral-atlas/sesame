{
    "private": true,
    "workspaces": [
        "api",
        "api-models",
        "cli",
        "client",
        "models",
        "www"
    ],
    "scripts": {
        "dev:www": "cd www; npm run dev",
        "dev:api": "cd api; npm run dev",
        "dev": "npx concurrently npm:dev:www npm:dev:api"
    },
    "devDependencies": {
        "concurrently": "^6.0.2",
        "nodemon": "^2.0.7"
    }
}
