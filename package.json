{
    "private": true,
    "workspaces": [
        "api",
        "api-models",
        "cli",
        "client",
        "models",
        "www",
        "example-app"
    ],
    "scripts": {
        "dev:www": "cd www; npm run dev",
        "dev:api": "cd api; npm run dev",
        "dev:ex-app": "cd example-app; npm run dev",
        "dev": "npx concurrently npm:dev:*"
    },
    "devDependencies": {
        "concurrently": "^6.0.2",
        "nodemon": "^2.0.7"
    }
}
