name: "Publish @astral-atlas/sesame-data"

on:
  push:
    tags:
      - '@astral-atlas/sesame-data@*'

jobs:
  publish-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 16
          registry-url: https://registry.npmjs.org/
      - run: npm ci
      - run: npm publish --access public -w data
        env:
          NODE_AUTH_TOKEN: ${{secrets.npm_token}}
