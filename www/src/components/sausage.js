// @flow strict
/*:: import type { Node } from 'preact'; */
import { h } from 'preact';

/*::
export type SausageProps = {|
  title: string,
  subtitle: string,
  content: Node,
  action: string,
  onSausageAction: () => mixed,
|};
*/

export const Sausage = ({}/*: SausageProps*/)/*: Node*/ => {

}